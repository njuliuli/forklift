---

test-debian:
  # this section fail to install foreman in the VM
  box_name:   'debian/stretch64'
  image_name: !ruby/regexp '/Debian.*Stretch.*PVHVM/'
  pty:        true
  scenarios:
    - foreman

  # box: debian9
  # memory: 6000
  # cpus: 4
  hostname: foreman
  networks:
    - type: 'public_network'
      options:
        # ip: 192.168.1.11
        type: "bridge"
        dev: "br1"
  ansible:
    playbook: 'user_playbooks/foreman.yml'
  #   group: 'devel'
  #   variables:
  #     katello_devel_github_username: <REPLACE ME>

...
